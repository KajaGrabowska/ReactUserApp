const Wrapper = (props) => {
  return props.children; //children prop holds all the content that you are passing 
                        //between the opening and closing tags of your custom component
};

export default Wrapper;
